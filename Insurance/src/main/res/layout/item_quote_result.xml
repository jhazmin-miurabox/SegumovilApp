<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="service"
            type="com.cursosant.insurance.common.entities.ServiceResponse" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="@dimen/common_padding_min">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/common_padding_min">
            <TextView
                android:text="@{service.name}"
                tools:text="Nombre de la aseguradora"
                style="@style/TextItemBody.Strong"/>

            <TextView
                android:layout_marginTop="@dimen/common_padding_min"
                android:text="@{service.description}"
                tools:text="Descripción"
                style="@style/TextItemBody.TwoLines.Center"/>

            <TextView
                android:layout_marginTop="@dimen/common_padding_min"
                android:text="@{service.totalAmount}"
                tools:text="$Monto total"
                style="@style/TextItemBody.Strong"/>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>