<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="coverage"
            type="com.cursosant.insurance.common.entities.PolicyCoverage" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="@dimen/common_padding_min"
            android:paddingHorizontal="@dimen/common_padding_min">
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="100"
                android:maxLines="1"
                android:ellipsize="end"
                android:textAppearance="?attr/textAppearanceSubtitle1"
                android:gravity="center_vertical"
                android:text="@{coverage.coverage_name}"
                tools:text="Nombre de la covertura"/>
            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@color/white"
                android:importantForAccessibility="no"
                tools:src="@drawable/ic_price_check"
                app:tint="?attr/colorTertiary"
                app:imgSubramo="@{coverage.subBouquet}"/>
        </LinearLayout>

        <include layout="@layout/content_field_coverage"
            app:subtitle="@{@string/policy_detail_coverage_sum}"
            app:description="@{coverage.sum}"/>

        <include layout="@layout/content_field_coverage"
            app:subtitle="@{@string/policy_detail_coverage_deductible}"
            app:description="@{coverage.deductibleValid}"/>

        <!--<include layout="@layout/content_field_coverage"
            app:subtitle="@{@string/policy_detail_coverage_coinsurance}"
            app:description="@{coverage.coinsurance}"/>

        <include layout="@layout/content_field_coverage"
            app:subtitle="@{@string/policy_detail_coverage_cap}"
            app:description="@{coverage.topecoinsurance}"/>-->
    </LinearLayout>
</layout>